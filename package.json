{
  "name": "localpage-factory",
  "version": "0.1.0",
  "description": "Transform structured local business JSON into a publish-ready SEO landing page",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "localpage-factory": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "fast-check": "^3.19.0"
  }
}
